#!/usr/bin/env python

from setuptools import setup, find_packages

setup(name='research_seed',
      version='0.0.1',
      description='',
      author='',
      author_email='',
      url='',
      install_requires=[
            'pytorch-lightning'
      ],
      packages=find_packages()
      )

